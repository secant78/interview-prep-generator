# Populated automatically by provision.yml.
# You can also set EC2_HOST manually after provisioning:
#   [app]
#   1.2.3.4 ansible_user=ec2-user ansible_ssh_private_key_file=~/.ssh/interview-prep-key.pem

[app]
# placeholder — provision.yml writes the real IP here
